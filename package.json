{
  "name": "careerhound",
  "version": "1.0.0",
  "private": true,
  "description": "Career Hound - 1:1 Direct ATS Job Board",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:seed": "npm --prefix backend run db:seed",
    "db:setup-neon": "npm --prefix backend run db:setup-neon",
    "db:ingest": "npm --prefix backend run db:ingest",
    "deploy:pages": "npm --prefix frontend run build && npx wrangler pages deploy frontend/dist --cwd frontend --project-name careerhound --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "better-auth": "^1.7.5"
  }
}
